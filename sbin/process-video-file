#!/bin/bash
# Based on https://www.reddit.com/r/PleX/comments/6gcfok/comskip_ffmpeg_script/

# Include script setup file
	source script-setup
	if [ ! $FUNCTIONS_ARE_DEFINED ]; then echo $0: Functions not defined. Aborting; logger $0: Functions not defined. Aborting; exit 1; fi
	if [ ! $VARIABLES_ARE_DEFINED ]; then echo $0: Variables not defined. Aborting; logger $0: Variables not defined. Aborting; exit 1; fi

# POSSIBLE OPTION: USE IF YOU WANT
# Require root
#	f_require_root

# PATH is unnecessary since it's already there, but it doesn't hurt anything.
set PATH = "$PATH:/usr/local/bin"
set ComChapPath = "/usr/local/bin/compath"
set ComChapFile = "$ComChapPath/compath"
set ComCutFile = "$ComChapPath/comcut"
set PVFlockFile = "/tmp/process-video-file.lock"
set ComchaplockFile = "/tmp/comchap.lock"
set origFile = "$1"
set tmpFile = "/tmp/$1.tmp"
set tmpEncode = "/tmp/$1.mkv"
set tmpSrt = "/tmp/$1.srt"
set dvrPostLog = "/media/8eeb64f5-b001-47d0-a261-bbee8a83d11d/plexdvr/dvrProcessing.log"

exit 1

#Wait if post processing is already running
while ( -f $PVFlockFile )
    echo "'$PVFlockFile' exists, sleeping processing of '$origFile'" # | tee $dvrPostLog
    sleep 10
	end

exit -1

#Mark and cut commercials
# echo "cut from '$origFile'" | # tee -a $dvrPostLog
# $ComCutFile --lockfile='ComchaplockFile' --comskip-ini=/media/8eeb64f5-b001-47d0-a261-bbee8a83d11d/plexdvr/comskip.ini "$origFile"




#Remove lock file
echo "Done processing '$origFile' removing lock" | tee -a $dvrPostLog
rm $PVFlockFile

# Include script footer file
	f_debug_variable "script_footer" "${PATHNAME[script_footer]}"
	source "${PATHNAME[script_footer]}"

exit 0

