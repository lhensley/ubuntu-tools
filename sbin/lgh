#!/bin/bash
# lgh

# Include script setup file
	source script-setup
	if [ ! $FUNCTIONS_ARE_DEFINED ]; then echo $0: Functions not defined. Aborting; logger $0: Functions not defined. Aborting; exit 1; fi
	if [ ! $VARIABLES_ARE_DEFINED ]; then echo $0: Variables not defined. Aborting; logger $0: Variables not defined. Aborting; exit 1; fi

# Export cron jobs for $CRONTAB_BACKUP_USERS
for THIS_USER in $CRONTAB_BACKUP_USERS; do 
	# crontab -l -u $THIS_USER > ~$THIS_USER/.crontab-archive
	# chown $THIS_USER ~$THIS_USER/.crontab-archive
	# chmod 600 ~$THIS_USER/.crontab-archive
	echo ~$THIS_USER/.crontab-archive
	echo $(echo ~$THIS_USER)/.crontab-archive
	done

# # tar
# 	if [ $COLD_BACKUP_FS_READ_ONLY ]; then f_mount_ro_fs_as_rw "$COLD_BACKUP_FILE_SYSTEM"; fi
# 	mkdir -p "$THIS_ARCHIVE_DIRECTORY"
# 	echo nice tar $BACKUP_OPTIONS $COLD_BACKUP_DIRECTORY
# 	nice tar $BACKUP_OPTIONS $COLD_BACKUP_DIRECTORY
# 	if [ $COLD_BACKUP_FS_READ_ONLY ]; then f_mount_ro_fs_as_ro "$COLD_BACKUP_FILE_SYSTEM" $COLD_BACKUP_FS_READ_ONLY; fi

# Include script footer file
	f_debug_variable "script_footer" "${PATHNAME[script_footer]}"
	source "${PATHNAME[script_footer]}"

exit 0